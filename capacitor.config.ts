import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: "Romeo's Ionic App",
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
